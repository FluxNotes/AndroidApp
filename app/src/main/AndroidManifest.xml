<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.mitre.fluxnotes">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DisplayResultsActivity"
            android:parentActivityName=".MainActivity" />

        <receiver
            android:name=".receivers.NLPRequestReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="org.mitre.fluxnotes.NLP_REQUEST" />
            </intent-filter>
        </receiver>

        <service
            android:name=".services.SpeechService"
            android:enabled="true" />
        <service
            android:name=".services.NLPService"
            android:enabled="true" />

        <activity
            android:name=".data.session.SessionListActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".data.session.DisplaySessionActivity"></activity>
    </application>

</manifest>